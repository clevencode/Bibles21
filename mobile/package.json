{
  "name": "bible-s21-mobile",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/prepare-www.mjs && vite build",
    "sync": "npm run build && npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor-community/sqlite": "^7.0.1",
    "@capacitor/android": "^7.4.2",
    "@capacitor/core": "^7.4.2",
    "jeep-sqlite": "^2.8.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.2",
    "vite": "^6.3.5"
  }
}
